# 救護紀錄表查詢工具的登入帳密（選填）
#
# 用法：把這個檔案複製成同目錄下的 .env（該檔已被 .gitignore 排除，不會進版控）。
# 不設定也可以，開啟瀏覽器後自己手動打帳號密碼即可；驗證碼一律要本人輸入。
#
# ⚠ 絕對不要加 VITE_ 前綴，也不要寫進專案根目錄的 .env——
#   根目錄 .env 是給網頁前端用的，VITE_ 開頭的變數會被打包進公開的網頁檔案。

EMS_USERNAME=
EMS_PASSWORD=

# ── 增減資料的 Google Sheet（用來調整統計數字）────────────────────
# 把整個網址貼在 = 後面即可（含 /edit#gid=... 也沒關係，程式會自己解析）。
# 例如：EMS_ADJUST_SHEET_URL=https://docs.google.com/spreadsheets/d/1AbC.../edit#gid=0
#
# 這個檔案不進版控，網址不會外流到 GitHub。
EMS_ADJUST_SHEET_URL=

# 選填：指定要讀哪個分頁的 gid。留白則使用網址裡的 gid，網址也沒有就讀第一個分頁。
EMS_ADJUST_SHEET_GID=

# ── 線上解鎖工單（unlock-online）────────────────────────────────
# 各分隊在網頁上提出的解鎖申請，要靠這組帳號去拿工單、回寫結果。
#
# ⚠ 這是**網頁系統**（ems-system-su1220.web.app）的帳密，
#   不是救護系統的帳密（那組是上面的 EMS_USERNAME）。
#
# ⚠ 用 Google 登入的人請注意：Google 登入**沒有密碼可以填在這裡**
#   （那組密碼是 Google 的，本系統拿不到也不該拿）。
#   請照下面四步，另外開一組專門給程式用的帳號：
#
#   1. 開 https://ems-system-su1220.web.app/register
#      （是「建立新帳號」那一頁，不要按 Google 登入）
#   2. 隨便填一組只給程式用的帳密，例如：
#        顯示名稱：解鎖工單程式
#        Email    ：你的帳號+unlock@gmail.com  ← Gmail 的 + 別名，信會進你原本的信箱
#        密碼      ：自己想一組（至少 6 碼），填到下面
#   3. 用你平常的 Google 帳號登入 → 使用者管理 → 把剛才那個帳號**核准**，
#      角色維持「一般使用者」（解鎖專用帳號沒有回寫結果的權限，不能用那個）
#   4. 把帳密填到下面兩行，存檔
#
#   為什麼要另開一組而不是用你自己的：這個檔案放在辦公室電腦上，
#   萬一外流，被拿走的只是一個一般使用者帳號，你隨時可以在使用者管理停用它。
EMS_WEB_EMAIL=
EMS_WEB_PASSWORD=
