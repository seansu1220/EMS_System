# 「開通大量傷患系統權限」工具的設定
#
# 用法：把這個檔案複製成同目錄下的 .env（該檔已被 .gitignore 排除，不會進版控），
#       再把下面幾行填好。
#
# ⚠ 這是**另一個系統**的設定，跟 tools/ems-report/.env（救護紀錄表查詢工具）沒有關係：
#   網址不同、帳號不同，兩邊各讀各的檔案，不會互相影響。
#
# ⚠ 絕對不要加 VITE_ 前綴，也不要寫進專案根目錄的 .env——
#   根目錄的 .env 是給網頁前端用的，VITE_ 開頭的變數會被打包進公開的網頁檔案。


# ── 要進去哪個網站（必填）────────────────────────────────────────
# 填這個系統的**登入頁**網址，整段貼上即可。
# 例如：MCI_ENTRY_URL=https://xxx.gov.tw/xxx/login.jsp
MCI_ENTRY_URL=


# ── 登入帳密（選填）──────────────────────────────────────────────
# 填了就會自動代填，省去每次打字；不填也可以，開了瀏覽器自己輸入即可。
#
# ⚠ 驗證碼一律由你本人在瀏覽器輸入，程式不做自動辨識（不繞過系統的防自動化機制）。
#   但只要登入一次，整份名單就會一路跑完，不必每個人重打一次；
#   而且登入狀態會存在 .auth/，8 小時內再跑一次連驗證碼都不用再打。
MCI_USERNAME=
MCI_PASSWORD=


# ── 預設單位（選填）──────────────────────────────────────────────
# 名單只寫姓名、沒寫單位時，就用這個單位。
# 每次的單位都不一樣的話這裡留空，改在名單裡逐行寫，或執行時加 --unit=單位。
#
# ⚠ 名稱要跟系統下拉選單裡的寫法一致；只寫一部分也可以，
#   但若同時符合兩個以上的選項，程式會停下來把候選列給你看，不會亂猜。
MCI_DEFAULT_UNIT=


# ── 開通大隊權限：要開通哪幾個單位（選填）──────────────────────
# 只有「開通大隊權限」那兩個捷徑會用到。
# 這裡列的單位，裡面的人全部開通 MCI002；其餘單位完全不碰。
#
# 留空＝第一～第四救災救護大隊＋特搜大隊（預設值）。要自己指定就用逗號隔開：
#   MCI_GRANT_UNITS=第一救災救護大隊,特搜大隊
#
# ⚠ 比對方式是「包含」，寫一部分也認得；一個關鍵字命中好幾個單位時全部都做。
# ⚠ 只要有**任何一個**寫法在系統的下拉裡找不到，程式會直接停手不做，
#   並把下拉裡實際的單位名稱印出來讓你照抄。
#   少開一個大隊很難事後發現（畫面上一切正常），寧可停下來。
MCI_GRANT_UNITS=


# ── 全面取消時要「保留」的單位（選填）────────────────────────────
# 只有「取消全部大量傷患權限」那兩個捷徑會用到。
# 這裡列的單位，裡面的人一個都不會被動到；其餘所有單位的人一律取消。
#
# 留空＝保留「緊急救護科」（預設值）。要留不只一個就用逗號隔開：
#   MCI_KEEP_UNITS=緊急救護科,火災預防科
#
# ⚠ 比對方式是「包含」，寫一部分也認得。
# ⚠ 保留單位若一個都沒在系統的下拉裡命中，程式會直接停手不做——
#   那通常代表名稱寫錯了，而繼續跑就等於把本來要留的單位也一起清掉。
MCI_KEEP_UNITS=
